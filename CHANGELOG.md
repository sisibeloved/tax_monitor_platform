# 版本历史

本文件记录集团所得税风险监测平台面向业务使用者和维护者的显著变更，不是 Git 提交记录的简单堆砌。

格式基于 [Keep a Changelog 1.1.0](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循[语义化版本 2.0.0](https://semver.org/lang/zh-CN/spec/v2.0.0.html)。在接入真实数据接口并完成试点前，版本保持在 `0.x`；版本号不代表生产批准。

## [未发布]

### 新增

- 增加季度第四项确定性能力“递延所得税计提/转回准确性检查”，按季度比较系统累计递延所得税费用与 SAP 累计已计提额，非零差异分别标记应计提或应转回。
- 增加递延所得税公司清单与公式证据展示，输出本年应计提/转回额、可弥补以前年度亏损、损益表累计利润总额、SAP 累计已计提额和递延所得税税率。
- 增加第六项核心能力“所得税退税进度监控及入账科目准确性检查”的领域契约：每年 3-12 月按月检查退税所属年 N、扫描年 N+1 的唯一贷方未冲销等额凭证行，区分正确科目、错误科目、未到账和多候选歧义。
- 增加退税监测标准输出：已退税公司清单、未退税公司清单、所得税退税金额和所得税退税入账科目；唯一命中其他收益时使用 `REFUND_BOOKED_TO_WRONG_ACCOUNT` 示警。
- 增加 SAP 利润表 DGC 连接器：支持华为 APIG AppKey/AppSecret 签名及 IAM Token 兼容认证，使用 `limitValue`/`offsetValue` 分页拉取真实长表数据，并将利润、累计营业收入和公允价值变动映射为受控季度指标。
- 增加 `POST /api/v1/ingest-batches/dgc-sap-profit`，复用既有批次幂等、公司主数据、期间/币种/精度校验、错误明细和来源记录入库链路。
- 增加“汇算清缴相关科目明细”数据表接口的独立安全配置，地址为 `/post/settlement_adjustment`，分页默认 `15000`；确认 `company/companyname/fiscal_year/fiscal_period/voucher_no/header_text/detail_text/amount_ksl/gl_account/account_name/project_code/project_name/debit_credit_flag/group_currency/original_system_doc_no` 共 15 个返回字段。
- 增加 `POST /api/v1/ingest-batches/dgc-sap-dividend-detail`，将严格筛选后的累计分红写入受控 `quarterly_metric/received_dividends` 批次，支持幂等 scope 哈希、季度截止和零值证据。
- 增加 `POST /api/v1/ingest-batches/dgc-sap-trial-balance`：固定查询 `6801010000`，一次拉取公司年度发生额并按季度截止本地归集，生成 `prior_quarter_current_tax` 和 `current_quarter_current_tax` 两个受控指标。
- 增加 `POST /api/v1/ingest-batches/dgc-sap-account-balance`：严格校验13字段真实响应，按十个 `224105` 科目的负期末余额转正汇总 `other_payables_accrual`，并按 `1811030000` 期末余额原值生成 `sap_cumulative_deferred_tax_expense`。
- 增加飞书多维表季度主数据字段映射，读取所得税税率、递延所得税税率、可弥补亏损额合计和3年平均税负率，仅保留公司代码非空且唯一的记录。

### 变更

- 将 SAP 科目余额表接口从已停用的 `/post/sapkemuyuebiao` 切换为 `/post/sapaccountbalance`，并同步新请求契约：`company_code` 可选，`fiscal_year`、`fiscal_period`、`limitValue`、`offsetValue` 必填。
- 核心监测范围更新为季度四项确定性检查、月度纳税调增科目准确性检查和“所得税退税进度监控及入账科目准确性检查”，共六项能力；历史 `QUARTERLY_V1` 仍按原三项季度规则回放，新规则版本运行四项季度检查。
- 退税金额按公司币种和 amount scale 采用 `ROUND_HALF_UP` 后完全等额匹配；工作簿 AJ 列原始值存在 4 位小数但按 2 位显示，该暂定口径须经业务验收。
- 退税公司下月是否跳过以平台数据库中公司+退税所属年的 `RECEIVED` 状态为准；飞书状态通过幂等 outbox 异步同步，不作为主事实。
- 退税台账已切换为飞书多维表：按公司代码精确查找唯一记录，将“是否已收到退税”幂等回写为“已退税”；零条或多条匹配均失败并进入有限重试，不猜测目标记录。
- 新增专用退税回写 Worker，飞书 App 凭据仅注入该进程；平台数据库和 outbox 继续作为主事实，远端失败不回滚已完成的税务扫描。
- 递延所得税计算保留“可弥补以前年度亏损 + 损益表累计利润总额”的已确认公式，正数表示应计提、负数表示应转回；SAP 费用/计提按正数、收益/转回按负数进入计算。
- 递延所得税税率作为独立受控主数据维护；SAP累计已计提递延所得税费用按科目余额表 `1811030000` 的期末余额原值读取，查询成功但目标科目缺失时按未计提处理并生成有来源证据的0，接口失败或未导入仍按缺数据处理。
- 当损益表累计营业收入小于或等于0时，本年累计所得税税负率直接取0，并继续计算与历史平均税负率的偏离度及预警方向。
- DGC 接口使用真实 `sapincome` 地址；AppKey/AppSecret、可选 IAM 凭据、分类账、源字段及 `ztext` 指标标签均由部署环境注入，仓库不保存真实凭据或 Token。
- DGC 利润表批次强制校验源财年/期间和指定公司范围，仅使用 `nyhsl` 累计金额，拒绝重复指标行；查询参数哈希纳入幂等身份，并限制单页字节数和总记录数，上游错误消息不再直接透传。
- 累计分红取数限定五个投资收益科目编码精确匹配，并要求头摘要或明细摘要包含“分红”“股利”或“利润分配”；金额使用 `Decimal` 计算 `sum(amount_ksl) * -1`，不使用浮点数，也不依据借贷标志再次翻转，并拒绝超出 `NUMERIC(38,12)` 的单行或累计金额。
- DGC 自签名证书接入改为固定叶证书 SHA-256 指纹并使用 `dgc.huaweicloud.com` SNI 校验；证书变化会在发送凭据前失败，客户端禁用环境代理和重定向并复用 HTTPS 连接。明文 HTTP 仍被接口以 `DLM.4474` 拒绝。
- 累计分红真实 scoped 响应确认省略 `company/companyname`，平台兼容该精确 13 字段形态并保留完整 15 字段校验；季度导入只累计至 3、6、9、12 月截止期，避免历史重跑混入后续月份。
- 利润表累计利润总额及累计公允价值变动损益增加变量取数工作簿给出的完整项目名称精确匹配；科目发生额的借贷金额保留源符号直接相加，不做二次方向转换。
- 利润表累计营业收入增加“`一、营业总收入`”精确匹配；汇算清缴相关科目明细增加 `6112010000/6112020000/6112040000` 其他收益取数并按 `-KSL` 汇总。
- 汇算清缴相关科目明细增加 `6801010000/6801020000/6801030000` 所得税费用发生额取数；每条明细独立按 `-KSL` 计算并保留来源字段，不跨明细汇总，同时复用季度截止、作用域和严格字段校验。
- 真实接口兼容性补充：利润表识别全角格式的公允价值变动收益项目；汇算清缴相关科目明细把可选文本字段的 `null` 规范为空字符串，业务身份字段仍保持严格校验。

### 计划

- 接入 SAP、OA、合思及受控线下主数据的真实接口或正式交换文件。
- 取得 SAP 退税凭证行缺失字段（行项目唯一标识、过账日期和冲销标志），完成金额精度业务验收，并确认飞书应用拥有目标 Base 的记录读取/编辑权限后再启用退税监测；所得税费用和其他收益精确科目清单已确认。
- 使用真实冻结公司快照完成试点 UAT、生产 KMS/HSM 签名和四方实名批准。
- 根据人工复核反馈持续治理金标、规则、提示词、模型适配器和科目字典版本。

## [0.4.0] - 2026-07-13

第四阶段完成平台治理、安全、运维、容量、发布和回滚加固，形成可供真实试点使用的技术基线。当前证据仍为 `LOCAL_SYNTHETIC`，生产准入结论保持 `false`。

### 新增

- 增加集团税务、公司财务、审计和受限服务身份的权限策略，并在 API、PostgreSQL 行级安全、工作任务和证据读取层统一执行公司范围。
- 增加仅追加审计事件，记录敏感读取、风险处理、导出、模型调用和控制面操作。
- 增加异步安全导出：冻结创建时权限、渲染时重新鉴权、下载前再次鉴权，并执行到期和保留策略。
- 增加企业模型网关，对业务招待费、福利费和捐赠 Agent 统一进行字段最小化、供应商策略、调用审计和敏感字段过滤。
- 增加健康检查、指标、运维驾驶舱以及数据错误、技术失败和税务风险的分类告警。
- 增加 126 家公司容量验收、受限并发、单公司失败隔离、仅失败公司重跑和最多 48 小时月度交付门禁。
- 增加签名发布清单、历史重放、迁移校验、可续跑回滚演练、试点 UAT 评分卡和集团分批推广手册。
- 增加独立数据库所有者和应用运行角色，API 与四类工作进程使用受限应用角色，迁移使用所有者角色。

### 变更

- 工作任务使用与队列、运行类型、批次、公司和期间绑定的签名服务范围；生产环境对缺失、篡改或越权任务默认拒绝。
- 风险清单、检测明细、审计、导出和模型证据均按请求主体和授权版本重新裁剪。
- 发布门禁固定镜像摘要、Git 提交、迁移头、规则、提示词、模型、科目字典、案例库和评估证据摘要。

### 修复

- 修复季度汇总重试只携带失败公司范围、无法观察完整批次终态的问题，同时将汇总重试改为有上限重试。
- 修复运维聚合查询在事务结束后访问已分离对象的问题。
- 强化审计和导出完整公司范围的行级安全表达，防止跨公司读取。

## [0.3.0] - 2026-07-13

第三阶段增加福利费与公益性捐赠专业 Agent，形成三类月度纳税调增科目的统一复核流程。

### 新增

- 增加福利费范围门禁，仅检查“累计福利费－累计工资薪金×14%＞0”的公司。
- 增加公益性捐赠范围门禁，仅检查“累计公益性捐赠－累计利润总额×12%＞0”的公司。
- 增加福利费 SAP 凭证 Agent，识别客户/供应商接待、培训讲师考试、宣传赠品、客户礼品、合理员工福利和证据不足场景。
- 增加公益性捐赠 SAP 凭证 Agent，识别赞助、冠名、品牌露出、广告权益、无对价公益捐赠和证据不足场景。
- 增加统一月度语义运行、结果信封、风险事项、证据引用、人工复核、关闭和重跑路径。
- 为福利费和公益性捐赠分别增加至少 50 条经财务、税务独立复核并完成裁决的冻结金标样本。

### 变更

- 三类月度监测共享受控科目字典、规则、提示词、模型适配器和案例库版本，但保留独立监测类型和幂等键。
- 福利费与公益性捐赠只接受 SAP 明细作为规范主记录，不使用 OA 或合思单据冒充 SAP 凭证。
- 风险结果统一输出当前科目、推荐科目、判断理由、证据、置信度和待补材料。

### 修复

- 隔离月度语义集成测试状态，避免不同监测主题和重跑场景互相污染。
- 防止高置信度预测数量为零时以空集准确率通过发布门禁。

## [0.2.0] - 2026-07-13

第二阶段增加业务招待费 Agent，将 SAP、OA 和合思业务证据纳入可追溯的月度科目准确性检查。

### 新增

- 增加业务招待费公司清单、月度累计期间和全量明细范围控制。
- 增加 SAP 业务招待费、合思业务招待报销、OA 业务招待申请、自采报销和物料领用数据接入及来源血缘。
- 增加精确关联、SAP 已关联、业务单据未关联和 SAP 覆盖状态等证据模式。
- 增加候选词典和专业 Agent，识别内部会议餐、培训餐、员工聚餐、团建、年会、加班餐、食堂、员工福利、会议通知、签到和议程等场景。
- 增加风险清单、改账建议、证据补充、人工复核、案件合并和安全导出体验。
- 增加经财务、税务独立复核并完成裁决的冻结业务招待费典型样例。

### 变更

- SAP 凭证能关联前置流程时，以 SAP 行为主记录并引用 OA/合思证据。
- OA 或合思单据无法关联 SAP 时，仍按申请事由独立判断，并标记“待定位 SAP 凭证”。
- 仅有 SAP 凭证但无法关联前置流程时，不把未关联本身直接判定为错账，而是输出覆盖状态或补证任务。
- 多来源命中同一事项时只形成一个根风险金额，避免重复统计税务风险。

### 修复

- 强化企业模型结构化输出、引用证据、科目允许清单和置信度门禁，防止模型补造事实或推荐未批准科目。
- 补齐业务招待费候选、关联、证据、重放和导出路径的幂等性。

## [0.1.0] - 2026-07-13

第一阶段建立集团季度所得税风险监测的确定性计算、受控数据和端到端运行基础。

### 新增

- 增加精确金额、税率和四舍五入领域类型，避免浮点误差和全局精度污染。
- 增加公司、批次、源记录、税务主数据、快照、规则版本、监测运行、检测结果和风险事项的数据模型。
- 增加公司主数据、季度财务指标和线下税务主数据导入、审批、有效期与质量控制。
- 增加不可变公司快照及快照集，固定来源批次、提取时间、公司范围和计算版本。
- 增加季度所得税计提准确性、累计税负率偏离和潜在纳税调增税务成本三类确定性监测。
- 增加跨公司 Celery 批次运行、单公司原子重试、部分成功、阻断原因和幂等风险事项。
- 增加按公司授权的季度风险 API、检测公式替换值、来源血缘和 React 驾驶舱。
- 增加 PostgreSQL、Redis、API、季度工作进程和 Web 的生产形态 Compose 拓扑。

### 变更

- 税率、可弥补以前年度亏损和前三个完整年度平均税负率改为从受控线下表按公司及有效期匹配。
- 累计分红金额明确取 SAP 账上收到分红的金额。
- 潜在调增金额统一为“其他应付款暂估余额＋合思无票报销金额”。
- 偏离度绝对值达到 5% 时触发累计税负率异常提示。

### 修复

- 强化季度持久化重试和批次最终化，防止失败重跑产生重复结果或批次长期停留在处理中。
- 固定冻结快照身份和文件血缘，确保重放使用原始受控证据而不是当前可变数据。
- 补齐控制面身份验证、公司范围和风险事项分派审计。
